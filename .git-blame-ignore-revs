# git blame から除外するコミット（整形だけで中身が変わっていないもの）。
#
# 使い方（リポジトリごとに一度だけ設定すれば、以後は自動で効きます）:
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# 1行に1つ、コミットの完全なハッシュ（40桁）を書きます。
# `#` で始まる行はコメントです。行末コメントは書けないため、
# どのコミットかは1行前のコメント行に書いてください。
#
# ruff format の一括適用（v0.5.0 後）
# ↑ コミット後に、そのハッシュをこの下の行に追記してください。
511125349bb981a2848a33cbf59f900bd9e772ac
