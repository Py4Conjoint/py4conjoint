# 開発メモ

将来の改善候補を記録しておく場所。ユーザー向けの変更履歴は `CHANGES.md`、
利用者への説明は docstring と `docs/` にあり、ここには**まだ着手しない**
（あるいは着手すべきか判断していない）話だけを書く。

---

## rating の D 最適交換アルゴリズムに「操作2」を入れるか

- 記録日：2026-07-27
- 対象：`src/py4conjoint/rating/design.py` の `_d_exchange_run()`
- 状態：**未着手**（既定の挙動が変わるため、今回のスコープ外とした）

### きっかけ

`auto_balance=True` の来歴 `df.attrs["auto_balance"]["det_ratio"]` が、
大きめの構成（5×4×3×3×2×2 の N=720 から 60 プロファイル）で **100.5%**
になった。「バランス制約を課したほうが det(X'X) が大きい」という、
一見おかしな値である。

### 原因

制約なし側と制約付き側で、探索が使える**手の種類が違う**ため。

| | 操作1（選択済み1行 ↔ 未選択1行の交換） | 操作2（選択済み2行のあいだで1属性の水準だけ入れ替え） |
|---|---|---|
| 制約なし `_d_exchange_run()` | あり | **なし** |
| 制約付き `_balanced_exchange_run()` | あり | あり |

操作2は `auto_balance` のために追加したものである。完全バランスな設計では
1対1の交換がどれも水準の出現回数を ±1 ずらしてしまい（max−min が 2 になる）、
操作1だけでは局所探索が完全に凍結する。それを解くために、水準の出現回数を
保ったまま動ける手として入れた。

結果として、**より広い実行可能領域を探索しているはずの制約なし側が、
狭い領域を探索している制約付き側に負ける**ことが起きる。制約の問題ではなく、
探索の質の問題である。

### 示唆

`auto_balance=False`（既定）の探索にも操作2を入れれば、同じように解が
改善する可能性がある。ただし **既定で返る設計が変わる**（`seed` を固定した
再現性も含めて）ため、変更するなら独立した判断が要る。

### 検討するときに確認すること

- 既定の設計が変わることの影響（教材・ノートブック・テストの期待値）
- 実行時間（現在の経路Bのプロファイルでは `_is_balanced` が約49%、
  `det_of` が約28%。制約なし側には `_is_balanced` がないので、
  操作2の追加は素直に det の計算回数を増やす）
- 改善幅の実測（どの構成でどれだけ det(X'X) が上がるか）

---

## choice 側の判定基準がまだ直書きである

- 記録日：2026-07-27
- 対象：`src/py4conjoint/choice/design.py` の `_check_balance()` /
  `_check_overlap()` / `_design_diagnostics()`
- 状態：**未着手**（今回は統一しない）

### 現状

rating 側は `check_design` の判定基準を
`src/py4conjoint/rating/analysis.py` の定数（`_BALANCE_CV_MAJOR` /
`_BALANCE_CV_MINOR` / `_CORRELATION_ABS_MAJOR` / `_CORRELATION_ABS_MINOR`）と
ヘルパー（`_balance_severity()` / `_correlation_severity()` /
`_cross_attribute_pairs()`）に一元化した。

choice 側は同じ数値が関数の中に直書きのまま残っている。

| 場所 | 数値 | 意味 |
|---|---|---|
| `_check_balance()` | `cv < 0.05` / `cv < 0.15` | 表示ラベル ◎/○/△ の境界 |
| `_check_overlap()` | `rate < 0.1` / `rate < 0.3` | 表示ラベル ◎/○/△ の境界 |
| `_design_diagnostics()` | `cv > 0.3` / `cv > 0.15` | バランス警告 [大] / [中] |
| `_design_diagnostics()` | `rate > 0.5` / `rate > 0.3` | オーバーラップ警告 [大] / [中] |

（行番号は変動するので関数名で示す。）

### 確認済み：`rate` は rating の `|r|` とは別の概念

`rate` は**セット内オーバーラップ率**（同一選択セット内の全代替案が同じ水準を
持つ設問の割合）であり、rating の属性間相関 `|r|` とは測っているものが違う。
閾値 0.5 / 0.3 が一致しているのは偶然である。**同じ数値だからという理由で
束ねてはならない。**

なお rating 側にも同種の偶然がある（`_check_balance()` の表示ラベル境界
`0.15` と警告基準 `_BALANCE_CV_MINOR` の一致）。そちらは該当箇所に
コメントを入れて注意を残した。

### 向き合うべき論点（choice に案A・案Cを展開するとき）

1. 統一するかどうか。判定基準が rating と choice で**同じであるべきか
   どうか自体が設計判断**である。設計の作り方（プロファイル単位 ／
   選択セット単位）が違うので、同じ CV でも意味が同じとは限らない。
2. 統一するなら、どこに置くか。両サブパッケージで共有する（共通モジュールに
   出す）のか、それぞれが自分の定数を持つのか。公開 API の対称性は保つ
   一方で、**基準の値まで連動させるべきかは別問題**。
3. `cv > 0.3` / `cv > 0.15` は rating と choice で偶然一致しているのか、
   意図して揃えたのかが不明。統一の前にどちらなのかを決める必要がある。

### なぜ今やらないか

案A・案C（`suggest_n_profiles` / `check_design` に auto_balance の存在を
伝える変更）は rating 側だけの作業であり、choice 側の直書きは今回の変更で
生じたものではない。上記1〜3は設計判断を伴うため、rating 側の実装が
固まってから改めて扱う。

その後の検討結果は「choice に案A・案Cを展開するか」にある。論点のうち、
`rate` が rating の `|r|` と同じ概念かどうかはそこで**解決した**（別概念）。

---

## `warnings()` の列が rating と choice で非対称になっている

- 記録日：2026-07-28
- 対象：`src/py4conjoint/rating/analysis.py` の `Diagnostic` /
  `DesignCheckResult.warnings()` / `ConjointResult.warnings()` と、
  `src/py4conjoint/choice/design.py` / `choice/analysis.py` の `warnings()`
- 状態：**意図して残している**（解消は choice への展開と同時に行う）

### 現状

案A・案Cで `Diagnostic` に `hint` フィールド（既定 `""`）を追加した。
「この n では回避できるのか」の案内文を `recommendation` に連結すると、
`warnings()` が返す DataFrame のセルに改行と字下げが `\n        ` という
エスケープ文字として入って読めなくなるため、別フィールドにしたもの。

その結果、返り値の列が食い違っている。

| | `warnings()` の列 |
|---|---|
| rating（`pcr`） | severity / category / message / recommendation / **hint** |
| choice（`pcc`） | severity / category / message / recommendation |

`Diagnostic` は rating・choice で**共有している**（choice 側は
`rating/analysis.py` から import している）ので、`hint` フィールド自体は
choice 側の `Diagnostic` にも存在する。ただし choice の `warnings()` は
自前で DataFrame を組んでおり、`hint` を列に入れていない。

### なぜ今そろえないか

- 常に空の `hint` 列は「果たされない約束」になり、なぜ値が出ないのかという
  別の疑問を生む
- choice への展開はこれから相談する話で、**やらないという結論もあり得る**。
  その場合、永久に空の列だけが残る
- 非対称の解消は、機能を実装するときに一緒に行うべきである
- `Diagnostic` は `__init__.py` に現れない内部クラスなので、フィールド追加の
  影響は限定的である（確認済み）

### 判断が必要になるとき

- **choice に案A・案Cを展開する場合**：そのとき choice の `warnings()` にも
  `hint` 列を足して揃える。判定基準をどこに置くかは
  「choice 側の判定基準がまだ直書きである」の論点1〜3と同じ議論になる。
- **展開しないと決めた場合**：この非対称を受け入れるか、`Diagnostic` を
  rating 用と choice 用に分けるかを判断する必要がある。

展開するかどうかの検討結果は、次の項目「choice に案A・案Cを展開するか」に
まとめてある。棚上げしている非対称の一覧もそちらにある。

---

## choice に案A・案Cを展開するか

- 記録日：2026-07-28
- 対象：`src/py4conjoint/choice/design.py` の `check_design()` /
  `_design_diagnostics()`、`design_choice_sets()`
- 状態：**検討済み・未着手**（v0.7.0 以降の候補。やらない判断もあり得る）

### 前提の違い：choice では全数列挙ができない

rating の判定は「その n で作れる設計を**全数列挙**する」ことに依存している。
choice は探索空間が桁違いで、同じ方法は取れない。

| | 規模 | 設計の総数 |
|---|---|---|
| rating の教材規模（3×2×2 の候補8通りから6プロファイル） | C(8,6) | **924 通り** |
| choice の教材規模（候補8通り、6設問 × 3選択肢） | — | **約 3.1 × 10^10 通り** |

したがって「警告の出ない設計が存在するか」を choice で**厳密に答えることは
できない**。案A・案Cをそのまま移植することは不可能である。

### choice の警告の内訳（確認済み）

| 警告 | 閾値 | rating との関係 |
|---|---|---|
| 水準バランス（CV） | 0.3 / 0.15 | 同じ概念 |
| 属性間の組み合わせの偏り | [中] | rating の相関に相当 |
| オーバーラップ率 | 0.5 / 0.3 | **choice 固有** |

オーバーラップ率は「設問の N% で全代替案が同じ水準になっている」という
choice 特有の欠陥で、rating の属性間相関とは別の概念である。0.5 / 0.3 が
rating の相関閾値と一致しているのは**偶然**。

これで、「choice 側の判定基準がまだ直書きである」に記録した
「`rate` が rating の `|r|` と同じ概念か確認できていない」は**解決**した
（別概念だった）。同じ数値だからという理由で束ねてはならない。

### rating と同じ問題は起きない

案A・案Cの動機は、rating の「プロファイルの組み合わせを見直してください」が
**実行できない指示**になり得ることだった（その n では構造的に避けられない場合）。

choice の助言は「設問数・バージョン数を増やす」「`seed` を変えて再生成」で、
設計がランダム生成である以上、どちらも実際に実行できる。同じ問題は起きない。

### choice 固有の問題

ただし `auto_balance=True` で**既に500候補を試したあと**に警告が残っている
場合、「`seed` を変えて再生成」は事実上役に立たない。500回の抽選に外れた人に
もう一度引けと言うのに等しい。ここが choice 版の「実行できない指示」である。

### 対処の案（未着手）

厳密な存在判定の代わりに**標本判定**を使う。K個（例：1000個）をランダムに
生成し、警告の出ない設計が何件あるかを数える。文言は「存在しません」と
断定せず「**見つかりませんでした**」と書く（`auto_balance` の経路A＝総当たり／
経路B＝発見的探索で確立した書き分けと同じ規律）。

決めるべきは **K の値**と、**何件で「見つからない」と言うか**。

### 棚上げの非対称（現時点で残っているもの）

1. `pcr` の `warnings()` は5列（`hint` あり）、`pcc` は4列
   → 詳細は前の項目「`warnings()` の列が rating と choice で非対称に
   なっている」
2. 判定の閾値が choice 側では直書きのまま
   → 詳細は「choice 側の判定基準がまだ直書きである」。ただし上記のとおり
   オーバーラップ率は rating に対応物がないので、**一律の共有はできない**
3. `check_design()` の置き場所が違う（rating は `analysis.py`、
   choice は `design.py`）

### 判断

v0.6.0 の内容は揃っているため、choice への展開は **v0.7.0 以降の候補**とする。
必須の修正ではなく、やらない判断もあり得る。

---

## 区間別 WTP は全体 WTP より不安定である（教材の主張を変える）

- 記録日：2026-07-28
- 対象：教材の説明（コード・データは変更しない）
- 状態：**決定済み**（見栄えの良い seed を選び直すことはしない）

### 決定事項

生成済みデータで区間別 WTP が理論値から大きくずれた。**seed を選び直して
きれいな数字を出すことはしない**。教材の主張を「区間別 WTP は全体 WTP より
不安定」に変える。

### 根拠（生成済みデータの実測。真値は仮定した構造から計算）

| | rating | choice | 真値 | 誤差 |
|---|---|---|---|---|
| 全体 WTP（6〜10万円） | 2.007 | 2.003 | 2.133 | 約 −6%（両手法とも） |
| 区間別 WTP 6〜8 | 3.646 | 4.204 | 4.571 | −20.2% / −8.0% |
| 区間別 WTP 8〜10 | 1.262 | 1.945 | 1.391 | −9.3% / **+39.8%** |

同じ100人・同じ真の選好・同じ調査から得た数字である。差は手法ではなく
**分母の不安定さ**から来る。区間別 WTP は隣接する価格係数の差を分母にする
ため、全体 WTP（分母 1.50 ÷ 4万円）より分母が小さく（6〜8 区間は 0.35 ÷
2万円）、係数のわずかな誤差が比を大きく動かす。

### 実証に seed 変更は不要

合成データなので真値が分かっており、**推定値と真値の比較そのもの**が
不安定さの実証になる。回答生成の seed を振って推定値のばらつきを見る実験は、
本物のデータではできない手続きなので教材には入れない。

### v0.7.0 の候補：WTP の不確実性表示

現在 `wtp()` は点推定のみを返し、パッケージ全体に信頼区間・標準誤差・
ブートストラップがない。`fit()` には `use_cluster` があり係数には SE が
あるが、**WTP は係数の比なのでそこから直接は出せない**（デルタ法か
ブートストラップが必要）。

実データで「もう一度調査したらどれだけ動くか」に答えるのは、回答者を
復元抽出して再推定するブートストラップ。合成データでの真値比較（教材）から、
実データの技法（ブートストラップ）への橋渡しになる。

実装時に注意すべき点：

1. **比の推定量は裾が重い。** ブートストラップ標本で価格係数が 0 に近づくと
   WTP が発散する。特に 6〜8 区間（真の落ち込みが 0.35 と小さい）で
   起きやすく、信頼区間が極端に広くなり得る。表示の設計が要る
2. **復元抽出の単位は回答者。** 行を抽出すると同一回答者内の相関を壊し、
   区間が狭くなりすぎる
3. `pcr` / `pcc` の**両方に同じ引数で**入れる

---

## バランス側の案内文が、判定の粒度と合っていない

- 記録日：2026-07-28
- 対象：`src/py4conjoint/rating/analysis.py` の `_balance_hint()`
- 状態：**未着手**（教材の経路では発火しないため v0.6.0 では直さない）

相関側の案内文を実データで読み直した際に、バランス側にも2件見つかった。
どちらも「判定を属性・ペア単位に拡張するとき」に一緒に扱う論点である。

### 状態A：判定は属性ごとなのに、文面は設計全体の話に読める

```
この n（6）ならバランスの取れた設計が存在します。
design_profiles(..., auto_balance=True) で解消できます。
```

判定は `judgment["auto_balance_balance"][attr]`、つまり **その属性の**
バランス警告が `auto_balance=True` の設計で消えるか、である。ところが
文面は「バランスの取れた設計」と書いており、設計全体がバランスすると読める。

属性 `os` だけ解消されて `price` は残る場合、案内どおりに
`auto_balance=True` を実行した利用者には「言われたとおりにしたのに警告が
減っただけ」に見える。

### 状態C：この n でも解消できることを書いていない

```
ただし auto_balance=True の設計でもこの偏りは残ります。
n_profiles を変えると解消する場合があります。
```

この分岐に入るのは `balance_avoidable[attr]` が真のとき、つまり
**この n でもバランスの取れた設計は存在する**（`auto_balance=True` が
det(X'X) を優先してそれを選ばないだけ）という状態である。相関側の
4つ目の状態は「両方を満たす設計は存在するが auto_balance は選ばない」と
書き分けているのに、バランス側だけこの情報が落ちている。

### 判断

どちらも `judgment` に必要な情報はすでに入っており、文言の問題である。
ただし状態Aを正確に書くには「他の属性はどうなるか」を案内文に含める必要が
あり、属性単位の書き分け（どの属性が解消し、どれが残るか）が要る。
`_correlation_hint` のペア単位化と同じ性質の拡張なので、
[choice に案A・案Cを展開するか] と合わせて判断する。
